import { Instance as CSS, Entity } from "cs_script/point_script";
import { expect, test } from "bun:test";
import MoveableSoundEvent from "./MoveableSoundEvent";


test("MoveableSoundEvent Init 1", () => {
    //let o = new MoveableSoundEvent(null, null);
});
